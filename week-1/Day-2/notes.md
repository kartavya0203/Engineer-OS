class variables are the variables thar are shared among all instances of the class, this should be the same for each class

when we access the class variable we need to access them through the class itself eg Employee.class variable or the instance of the class eg self.classvariable

when we try to access an attribute from the instance it first checks if the instnace contain that attribute or the class contains it or not 

we have self.raise_amount so that gives us ability to change single raise_amount as if we can do it for indivudal variable without changing the class variable

