What is a class 
-> A class is a blueprint basically which we can create and use it for the further usecases they allow us to logically group data and fucntions 

what is an object 
--> it represent the class instance and have a distinct address in the memory

what is self 
--> it is a refrence to the current object and it can be anything like for eg 
def fullname(banana):
      print(banana.first)

Why it exists

Python passes the current object as the first argument so methods know which object's data they should work with.